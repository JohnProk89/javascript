const add = (x, y) => { return x + y; };

const sum = (x, y) => x + y; // Curly braces and return type can be emmitted when single line body function
const hash = (n) => n

const poly = x => x*x + 2*x + 3; // Parentheses can be emmited if only one parameter exists

const constFun = () => 42; // Parentheses cannot be emmited if zero parameteres exist